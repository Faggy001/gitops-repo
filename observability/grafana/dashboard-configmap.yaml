apiVersion: v1
kind: ConfigMap
metadata:
  name: kubescape-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  kubescape-security.json: |
    {{ .Files.Get "dashboards/kubescape-security.json" }}
